# Mainnet deployment console boundary

The compliance agent can gather evidence and block incomplete deployments. It
cannot certify legal compliance, sign transactions, custody reserves, or enable
minting. Production activation requires the named human approvals in the
manifest, two independent wallet signatures, and a timelock.

The example manifest keeps token minting and bail/collateral custody disabled.
